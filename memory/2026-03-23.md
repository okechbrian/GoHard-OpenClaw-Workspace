# 2026-03-23 - Daily Notes

## Power Reporting Bot Redesign

### What Changed
- **Redesigned** `scripts/power-report-bot.py` from basic command-based approach to conversational flow
- Old flow: user enters `/report outage Kitoro "No power since morning"` — robotic, no clarity on incomplete reports
- New flow: conversational, asks clarifying questions before confirming

### How It Works Now
- User starts with `/start`, bot asks "Do you want to report a power outage or a broken line?"
- Based on answer, bot asks for:
  1. Location (where?)
  2. Details (when did it start, exact spot, what you see)
  3. Confirmation before posting to channel `-1003411154118`
- Bot also posts to channel with formatted report + pins it
- Tracks reports in `scripts/reports.json`

### Files
- **Bot:** `scripts/power-report-bot.py` (running in session `golden-toad`)
- **Docs:** `scripts/power-report-setup.md` (detailed setup & usage guide)

### API Callbacks
- `confirm_yes` → confirms and posts to channel
- `confirm_no` → cancels report

### Current State
- Bot is running (session `golden-toad`)
- Earlier multiple instances conflicting — killed all, restarted with single instance

## Channel Access Issue
- Brian asked subscribers to report "through the channel" Entebbe Power Alerts (`@poweralerts`)
- **Problem discovered:** Channel is broadcast-only (type: "channel"), so subscribers can't post messages
- Only admins can post — so the bot DM flow (subscribers message bot → bot posts to channel) is the correct approach
- Waiting for Brian's decision on whether to:
  1. Keep as channel (DM-based reporting via bot) 
  2. Convert to supergroup
  3. Add a linked discussion group

## Proxy
- Confirmed: `http://10.117.108.225:7071` is working for Telegram API calls

## GoHardVPN Build Progress (18:17)

### Fixes Applied
1. **Manifest duplicate attribute** - Removed `android:name="android.permission.POST_NOTIFICATIONS"` from AndroidManifest.xml (was defined twice: uses-permission + permission)
2. **Kotlin daemon crashes** - Added `kotlin.compiler.execution.strategy=in-process` to `gradle.properties` and increased JVM heap to `-Xmx4096m`
3. **Process.waitFor() signature** - Changed `xrayProcess?.waitFor(3000)` to `xrayProcess?.waitFor(3, TimeUnit.SECONDS)` (Java Process API requires TimeUnit)
4. **Process.pid() unavailable** - Removed `xrayProcess?.pid()` logging (not available in Android runtime)
5. **Import fix** - Added `import java.util.concurrent.TimeUnit`

### Current Status
- Build progressing: Kotlin compilation passed, Java compilation running as of 18:17
- Need to check final result and install APK on device
